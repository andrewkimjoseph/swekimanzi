Update the Days To Go work entry from the source project.

1. Copy the new logo from `@project:0fcfa613-c575-42fc-956a-32bf60abf04b` (`src/assets/logo.png`) into the current project as `public/logo-daystogo.png`.
2. Update `src/components/PlaceholderImage.tsx` so the `logo-daystogo` asset points to the new `.png` file instead of the old `.svg`.
3. Update `src/data/projects.ts` with the refreshed description: “A playful, brutalist countdown timer web app. Run as many clocks as you like — a quick 3-second timer or a date years out — ticking down to the second, with a burst of confetti and a chime when the timer hits zero. Local-first: your timers live in your browser, so there's no account and no server to trust. Put a clock on it.”

No other projects or styles are affected.
